---
  - hosts: all
    vars:
      - ansiblebot_password: '{{LPassword}}'
   gather_facts: no
   remote_user: sysop
   become: true
   tasks:
   - name: Add a new user named ansible-op
      user:
        name: ansible-op
           shell: /bin/bash
           password: "{{LPassword}}"